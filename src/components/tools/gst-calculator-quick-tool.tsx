'use client';
import GstCalculatorTool from './gst-calculator-tool';

export default function GstCalculatorQuickTool() {
  // This tool is functionally identical to the main GST calculator.
  // The "quick mode" can be a design variation or simply an alias.
  return <GstCalculatorTool />;
}


'use client';
import SIPCalculatorTool from './sip-calculator-tool';

export default function MutualFundReturnsCalculatorTool() {
  // This tool is functionally identical to the SIP calculator for projection purposes.
  // We can alias it or create a more specific UI later if needed.
  return <SIPCalculatorTool />;
}
